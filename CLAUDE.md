# CLAUDE.md

## 效率约束 Economy Constraints

以最小 token 消耗达成目标,同时不牺牲关键信息质量。

### 范围
- 只解决当前问题,不扩展无关内容,不做"顺便看看"的探索。
- 信息足够即动手;长任务先拆最小可行步骤,每步验证后再继续,避免返工。

### 上下文
- 使用已有上下文,禁止重复已知信息:不复述已定决策,不重新推导已确认事实,不重复读取已读文件。
- 仅在实际影响结论的信息缺失时提问;否则合理假设并明确说明假设。
- 不确定的信息不要猜,必要时先核实。

### 输出
- 内部充分分析,但只输出结论、关键依据和执行步骤,不输出冗长推理过程。
- 优先给可执行方案:代码、参数、配置。多方案只比较关键差异,并直接推荐一个。
- 默认简洁;仅当用户明确要求详细时才展开。
- 去掉客套语、确认语和收尾总结。
- 用户提供代码/日志/截图时,直接针对其内容分析,不复述原文。

### 读取与搜索
- 大文件只读所需区间(offset/limit),禁止整读。
- 查找用 Grep 精确命中(head_limit/-o/count),列路径用 Glob,禁止 cat/find 全量倒出。
- 跨文件搜索交给子代理,只回收结论;单个已知符号/文件直接查,不派代理。

### 并发
- 相互独立的工具调用放在同一条消息并行发出。
- 利用 prompt cache(~5 分钟 TTL):连续工作集中做,不频繁翻篇。

### 保真(不可妥协)
- 不要为了省 token 删除关键技术细节、错误信息、边界条件或失败证据——汇报结果要如实。

### 例外
- 用户说"详细解释"或"给全部选项"时,解除上述精简约束。
